(function($) {
    // global variables

    $(document).ready(function() {
        // code executions
        name();
        // event listeners 
        $(document).on('click', 'selector', function() {
            
        });
    });

    // all the function definitions
    function name() {

    }
})(window.jQuery);